import BaseApplication from 'common/application';

/**
 * The server application is used to access resources on the 
 * local system -- primarily opening files, and synchronizing them with
 * the editor.  
 */

class ServerApplication extends BaseApplication {
  static fragments = [
    
  ];
}

export default ServerApplication;